-- analog-clock-calendar
-- by @wim66
-- v1 May 17, 2025

conky.config = {
    -- Window settings to make it floating, transparent, and click-through
    -- background = true,
    update_interval = 1.0,
    cpu_avg_samples = 2,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_stderr = false,
    extra_newline = false,

    -- X11 Window Rules
    own_window = true,
    own_window_type = 'utility', -- normal|desktop|dock|panel|utility|override
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_argb_value = 0,    -- 0 is fully transparent background
    own_window_hints = 'undecorated,above,sticky,skip_taskbar,skip_pager',
    minimum_width = 50, minimum_height = 50,

    -- Alignment on screen (Options: top_left, top_right, bottom_left, bottom_right, top_middle)
    alignment = 'br',
    gap_x = 10,
    gap_y = 10,

    -- Aesthetics
    lua_load = './scripts/widget.lua',
    lua_draw_hook_pre = 'main',
    double_buffer = true,
    text_buffer_size = 4096,    
    use_xft = true,
    font = 'Ubuntu Mono:size=12',
    xftalpha = 1,
    override_utf8_locale = true,
    draw_shades = false,
    draw_outline = false,
    draw_borders = false,
    draw_graph_borders = false,
    default_color = 'white',
}

conky.text = [[

]]
